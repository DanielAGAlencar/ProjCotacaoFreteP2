@model IEnumerable<ProjCotacaoFreteP2.frete>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.codigo_rastreio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.data_postagem)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cotacao.valor)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.codigo_rastreio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.data_postagem)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cotacao.valor)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
